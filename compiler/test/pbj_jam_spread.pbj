master(map slaves, array args) {
	jam: doNext("lemon") spread: doFirst( "apple", 10, @[10,10,5]);
}

string doFirst( String s, Long l ) {
	print("Did something");
	-> 1;
}

string doNext(map slave, array args) {
	print("Did something else");
	print(args[0]);
	-> 1;
}
